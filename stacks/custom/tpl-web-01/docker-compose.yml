version: "3.8"

services:
  web:
    image: nginx:stable-alpine
    container_name: tpl-web-01
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8080}:80"
    volumes:
      - web_data:/usr/share/nginx/html

volumes:
  web_data:
